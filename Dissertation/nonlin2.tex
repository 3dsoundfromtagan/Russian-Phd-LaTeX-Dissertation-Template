\chapter{Фазовый синхронизм при фотоиндуцированной генерации второй гармоники в оптическом волокне}\label{ch:nonlin2}

\section{Постановка задачи}

Несмотря на изучение ГВГ в германосиликатных ОВ, ведущее свою историю с 1986 года \cite{osterberg1986dye}, динамика формирования решётки квадратичной восприимчивости в процессе оптического полинга исследована неполно. В частности, изменение кривых фазового синхронизма в процессе записи \(\chi^{(2)}\)-решётки, чувствительных к изменениям фазовых параметров решётки и условий фазового синхронизма, до настоящего времени экспериментально не изучалась. Информация о характере изменения кривых фазового синхронизма в будущем может дать понимание физических принципов формирования решётки квадратичной нелинейной восприимчивости, что впоследствии может быть использовано как для создания источников излучения на основе ГВГ в ОВ, так и для подавления нежелательной генерации в оптических системах. Кроме того, требуется верификация существующих теоретических моделей с точки зрения эволюций КФС.


В отличие от ряда работ, где для ускорения процесса записи \(\chi^{(2)}\)-решётки используется затравочное излучение второй гармоники~\cite{stolen1987self}, в настоящей работе исследуется эволюция спектральных и температурных КФС второй гармоники в процессе оптического полинга германосиликатного волокна излучением импульсного иттербиевого лазера в режиме самозатравки. Такой режим обусловлен практическим применением: в разрабатываемом источнике среднего ИК--диапазона на основе ГРЧ в н-о кристалле волоконный лазер накачки не содержит затравочного излучения второй гармоники. Для интерпретации полученных экспериментальных данных проведено их сравнение с численным моделированием на основе модели~\cite{antonyuk2001self}.
%В данной главе представлены результаты экспериментального исследования эволюции спектральных и температурных кривых фазового синхронизма (КФС) второй гармоники в процессе оптического полинга германосиликатного оптического волокна излучением импульсного иттербиевого лазера. Проведено сравнение полученных экспериментальных результатов в результатами численного моделирования, основанных на модели~\cite{antonyuk2001self}.

%Для достижения цели необходимо решить следующие задачи: разработать экспериментальную установку и провести оптический полинг волокна; в процессе формирования решётки на различных временных этапах измерить спектральные и температурные кривые фазового синхронизма ГВГ; определить динамику их основных параметров (сдвиг пика, ширина, эффективность); выполнить численное моделирование на основе существующей теоретической модели и провести сравнительный анализ полученных результатов.



\section{Экспериментальное исследование фазового синхронизма при ГВГ в ОВ}\label{sec:nonlin2/experiment}

В качестве исследуемого образца было выбрано германосиликатное ОВ FUD-3562 производства Nufern \cite{fud3562}. Параметры данного ОВ представлены в табл.

\begin{table}[htbp]
	\centering
	\caption{Параметры оптического волокна FUD-3562}
	\label{tab:fud3562_params}
	\begin{tabular}{@{}lc@{}}
		\toprule
		Параметр & Значение \\
		\midrule
		Диаметр сердцевины & \SI{10.5}{\micro\meter} \\
		$\Delta n$ & $1.9 \times 10^{-3}$ \\
		MFD на длине волны \SI{1030}{\nano\meter} & \SI{12}{\micro\meter} \\
		Количество поддерживаемых мод на длине волны \SI{1030}{\nano\meter} & 1 \\
		Количество поддерживаемых мод на длине волны \SI{515}{\nano\meter} & 9 \\
		\bottomrule
	\end{tabular}
\end{table}




Выбор ОВ с легированием сердцевины германием обусловлен высокой предрасположенностью подобных волокон к ГВГ~\cite{gemechu2023survey}. Данное волокно является одномодовым на длине волны накачки $1030$~нм и поддерживает распространение девяти поперечных мод на длине волны второй гармоники $515$~нм.


\begin{figure}[h]
	\centering
	\includegraphics[width=0.8\linewidth]{Dissertation/images/fibershg/scheme}
	\caption{Схема экспериментальной установки по исследованию кривых фазового синхронизма в условиях записи решётки нелинейной квадратичной восприимчивости. Чёрные участки ОВ: негерманосиликатное оптическое волокно, в котором генерация второй гармоники пренебрежимо мала. M: набор дихроичных зеркал для разделения волн накачки и второй гармоники; PM$_1$, PM$_2$: фотодиодный и тепловой измерители мощности соответственно.}
	\label{fig:shg_scheme}
\end{figure}

Упрощённая блок-схема экспериментальной установки представлена на рис.~\cref{fig:shg_scheme}. В качестве источника накачки использовался импульсный иттербиевый волоконный лазер. Длина волны источника перестраивалась в диапазоне от $1031.9$~нм до $1032.6$~нм, пиковая мощность достигала 4~кВт, длительность импульсов и частота их повторения составляли 2~нс и 5~МГц соответственно. Для разделения излучения второй гармоники и накачки использовался набор из трёх последовательно расположенных дихроичных зеркал M. Коэффициент отражения каждого зеркала на длине волны 1030~нм не превышал 1\%. Пиковая мощность остаточного излучения накачки на фотодиодном измерителе мощности не превышала 60 мкВт. Это значение было измерено в начале эксперимента, когда ГВГ отсутствовала. Исследуемое ОВ длиной 1~м выделено зелёным цветом на рис.~\cref{fig:shg_scheme}. Для контроля температуры во время оптического полирования ОВ было помещено в термостат, обеспечивающий точность стабилизации температуры не хуже $0.1~^{\circ}$C. Необходимость размещения исследуемого ОВ в термостате обусловлена тем, что, согласно литературным данным, решётка $\chi^{\left(2\right)}$ может формироваться на различных участках волокна \cite{batdorf1989study, lacerda1994grating}.
%По этой причине оптические сварки исследуемого ОВ с негерманосиликатными волокнами, в которых не наблюдалась ГВГ, также были помещены в термостат.
	
\begin{figure}[h]
	\centering
	\includegraphics[width=0.8\linewidth]{Dissertation/images/fibershg/vac}
	\caption{Зависимость эффективности ГВГ от средней мощности накачки}
	\label{fig:shg_vac}
\end{figure}


Поскольку высокая интенсивность излучения второй гармоники приводит к перезаписи ранее сформированной \(\chi^{(2)}\)-решётки \cite{ouellette1988light}, была экспериментально оценена максимальная мощность излучения накачки, при которой решётка ещё остаётся неизменной. \(\chi^{(2)}\)-решётка была записана в на участке ОВ, аналогичном исследуемому,  после чего измерялась зависимость эффективности преобразования от мощности накачки. Данная зависимость представлена на рис.~\cref{fig:shg_vac}. Примечательно, что для одного и того же значения мощности накачки эффективность ГВГ при её снижении и повышении была различной. Это говорит об изменении свойств \(\chi^{(2)}\)-решётки при воздействии на неё мощного излучения накачки. При увеличении мощности накачки упомянутая зависимость оставалась линейной вплоть до значения 500 Вт. В дальнейшем мощность накачки, используемую для измерения кривых фазового синхронизма, поддерживали значительно ниже этого уровня, а именно на уровне 50 Вт.


Методика эксперимента была следующей:

\begin{enumerate}
	\item Температура ОВ и длина волны накачки устанавливались равными  $22^{\circ}$C и $1032.42$~нм соответственно;
	\item Излучение накачки пиковой мощностью \(4\)~кВт пропускали через ОВ для записи решётки \(\chi^{(2)}\);
	\item \label{item:curve_meas} Во время облучения ОВ контролировалась мощность второй гармоники, и после достижения определенного уровня запускалась процедура измерения кривых фазового синхронизма:
	\begin{enumerate}
		\item Пиковая мощность накачки уменьшалась до 50~Вт;
		\item Для измерения спектральной кривой синхронизма длина волны накачки перестраивалась во всём доступном диапазоне Температура ОВ при этом поддерживалась равной $22^{\circ}$C;
		\item \label{item:temperature_tuning_curve} Длина волны накачки устанавливалась равной $1032.42$~нм. Для измерения температурной кривой синхронизма изменялась температура ОВ;
		\item \label{item:pump_power_increase} Мощность излучения и длина волны накачки, а также температура ОВ устанавливались равными 4~кВт, $1032.42$~нм и $22^{\circ}$C соответственно;
	\end{enumerate}
	\item Пункт (\ref{item:curve_meas}) последовательно повторялся для различных значений мощности второй гармоники до тех пор, пока она не достигала стационарного уровня.
\end{enumerate}

Для проверки воспроизводимости описанный выше эксперимент был проведён на двух одинаковых образцах ОВ. Поскольку полученные результаты оказались практически идентичными, приведены данные только для одного из образцов.

После каждого эксперимента проверялось, что \(\chi^{(2)}\)-решётка сформировалась только в исследуемом ОВ. Для этого участок исследуемого ОВ исключался из схемы, и излучение накачки распространялось только по негерманосиликатным ОВ. При этом ГВГ не наблюдалась.

Также было установлено, что \(\chi^{(2)}\)-решётка не претерпевала заметной релаксации в течение примерно одного месяца. Об этом свидетельствовало отсутствие существенных изменений кривых синхронизма.


\clearpage

\section{Математическая модель фазового синхронизма при ГВГ в ОВ}

Согласно модели Б.~Антонюка и В.~Антонюка \cite{antonyuk2001self}, ГВГ в ОВ описывается системой трёх дифференциальных уравнений первого порядка. Два уравнения описывают взаимодействие между волнами накачки и второй гармоники, а третье~--- временную эволюцию квазистатического электрического поля, ответственного за формирование \(\chi^{(2)}\)-решётки.

Для того чтобы учесть эффект Керра, который в данном случае не является пренебрежимо малым, исходная модель \cite{antonyuk2001self} была расширена путем добавления соответствующих нелинейных членов. В оригинальной работе данный эффект не рассматривался, однако в условиях нашего эксперимента его вкладом пренебрегать нельзя. Для ОВ c нелинейным показателем преломления $n_{nl} = \SI{2.5e-20}{\watt\per\meter\squared}$, эффективной площадью моды $A_{eff}\sim80~\text{мкм}^2$, длиной $L=1$~м, показателем преломления $n_{\omega}=1.45$ на длине волны $\lambda=1030$~нм при мощности накачки $P=4$~кВт получаем нелинейный набег фазы

\begin{equation}
	\Delta\phi_{\text{nl}} = \frac{4\pi\omega n_{nl}}{c n_{\omega}} \frac{P}{A_{\text{eff}}} L \approx 8 \text{ рад}
	\label{eq:nl_phase_estimation}
\end{equation}


Это значение сопоставимо с ожидаемым изменением фазы квазистатического поля, также составляющим несколько радиан. Следовательно, учет керровской нелинейности необходим для корректного описания процессов взаимодействия волн в формирующейся \(\chi^{(2)}\)-решётке.


\begin{equation}
	\begin{aligned}
		\frac{\partial A_1}{\partial z} &= \im \frac{4 \pi \omega \chi^{\left(3\right)}}{c n_{\omega}}\left( A_0^{\ast} A_1^{\ast} A_2 + \gamma_{11} A_1 A_1^{\ast} A_1 + 2\gamma_{12} A_2 A_2^{\ast} A_1\right), \\
		\frac{\partial A_2}{\partial z} &= \im \frac{4 \pi \omega \chi^{\left(3\right)}}{c n_{2\omega}}\left(A_0 A_1^{2}  + 2\gamma_{12} A_1 A_1^{\ast} A_2 + \gamma_{22} A_2 A_2^{*} A_2 \right), \\
		\frac{\partial A_{0}}{\partial t} &= \alpha_2 \left|A_2\right|^2 \cdot \left(u e^{\im\left[\arg{A_2}-2\arg{A_1}\right]}-A_{0}\right).
	\end{aligned}
	\label{eq:anton}
\end{equation}

Здесь $A_0$, $A_1$, $A_2$ --- комплексные амплитуды квазистатического, поля накачки и поля второй гармоники соответственно, $c$ --- скорость света в вакууме, $\omega = 1.83 \cdot 10^{15}~\text{рад}/\text{с}$ --- угловая частота накачки, $n_{\omega} = 1.4553$, $n_{2\omega} = 1.4655$ --- показатели преломления ОВ на частотах накачки и второй гармоники соответственно~\cite{ghosh1994temperature}, $\chi^{(3)} = 10^{-19}~\text{м}^2/\text{Вт}$~\cite{fleming2008poled}, $u$ --- амплитуда насыщенного квазистатического электрического поля, $\alpha_2$ --- коэффициент, определяющий скорость насыщения, $\gamma_{11}$, $\gamma_{12}$, $\gamma_{22}$ --- коэффициенты, пропорциональные эффективным интегралам перекрытия мод~\cite{agrawal2019}.

Вариация параметров модели позволяет привести измеренные в эксперименте кинетику второй гармоники и кривые синхронизма в соответствие расчитанным. Значения параметров $u$ и $\alpha_2$ зависят от концентрации ионов Ge~\cite{gemechu2023survey, tsai1989correlation} и в литературе четко не определены. Максимально достижимая эффективность преобразования связана с параметром $u = 5~\text{статВ/см}$, что соответствует значению $1.5$~кВ/см в СИ. Комбинация $(\alpha_2 u^{2})^{-1} = 5~\text{часов}$ задает характерный временной масштаб процесса записи $\chi^{(2)}$-решётки.

\begin{figure}[ht]
	\centerfloat{
		\hfill
		\subcaptionbox{\label{fig:shg_real_beam}}{%
			\includegraphics[height=0.18\textheight]{Dissertation/images/fibershg/real}}
		\hfill
		\subcaptionbox{\label{fig:shg_calc_beam}}{%
			\includegraphics[height=0.18\textheight]{Dissertation/images/fibershg/calc_lp12}}
		\hfill
	}
	\caption{а): Профиль пучка второй гармоники в эксперименте б):Рассчитанная мода LP$_{12}$}\label{fig:lp_shg}
\end{figure}


Параметры $\gamma_{11}$, $\gamma_{12}$, $\gamma_{22}$ зависят не только от модового состава излучения накачки и второй гармоники, но и от поперечного распределения статического электрического поля, точное распределение которого для конкретной задачи без специального измерения, как, например, в работе \cite{dominic1994high}, неизвестно. Поэтому значения данных параметров также подбирались для наилучшего соответствия эксперименту. В качестве начального приближения были взяты значения, соответствующие моде накачки LP$_{01}$ и моде второй гармоники LP$_{12}$. Данные моды были выбраны потому, что исследуемое волокно является одномодовым на длине волны накачки, а экспериментально наблюдаемый профиль излучения второй гармоники обнаруживал сходство с профилем моды LP$_{12}$ (см. рис.~\cref{fig:lp_shg}). Поскольку численные значения данных коэффициентов определяют постоянный во времени сдвиг кривых фазового синхронизма, их величины были определены из условия наилучшего соответствия модельных данных экспериментальным. Полученные численные значения составили: $\gamma_{11} = 0.25$, $\gamma_{12} = 0.24$, $\gamma_{22} = 0.30$.

Начальные условия для системы уравнений~\eqref{eq:anton} были следующими:

\begin{equation}
	\begin{split}
		A_{0}(z, 0) &= 0~\text{статВ/см}, \\
		A_{1}(0, t) &= 3293~\text{статВ/см}, \\
		A_{2}(0, t) &= 0.052~\text{статВ/см}.
	\end{split}
\end{equation}


Амплитуды полей соответствуют начальным пиковым мощностям накачки $P_1(0) = 4$~кВт и второй гармоники $P_2(0) = 500$~нВт.

Начальное излучение второй гармоники в отсутствие затравочного излучения может быть обусловлено, например, ГВГ на границе сердцевина-оболочка. Максимальная эффективность такого преобразования не превышает $10^{-5}$~\cite{terhune1987second}. Поэтому параметр начальной мощности второй гармоники также является степенью свободы, позволяющей привести модельные результаты в соответствие экспериментальным, и это следует учитывать даже в случае отсутствия затравочного излучения на входе в исследуемое ОВ. Было установлено, что значение этого параметра влияет на кинетику кривых синхронизма. Начальная мощность второй гармоники варьировалась вместе с параметрами $u$, $\alpha_2$, $\gamma_{11}$, $\gamma_{12}$, $\gamma_{22}$ для достижения максимального соответствия с экспериментальными данными как по эффективности преобразования в стационарном состоянии, так и по времени, необходимому для достижения этого состояния.


Для моделирования измерений кривых фазового синхронизма необходимо учесть, что длина волны считывающего излучения (для спектральной кривой синхронизма) или температура ОВ (для температурной кривой синхронизма) более не равны соответствующим величинам, при которых велась запись решётки \(\chi^{(2)}\). Для этого в систему уравнений~\eqref{eq:anton} необходимо ввести ненулевую фазовую расстройку $\Delta k = k_2 - 2k_1 - 2\pi/\Lambda \neq 0$. Здесь $k_1$, $k_2$ --- волновые векторы накачки и второй гармоники соответственно; $\Lambda = 51$~мкм --- период квазисинхронизма $\chi^{(2)}$-решётки. Множитель, отвечающий за фазовую расстройку, можно явно ввести следующим образом:

\begin{equation}
	\begin{aligned}
		\frac{\partial A_1}{\partial z} &= \im \frac{4 \pi \omega \chi^{\left(3\right)}}{c n_{\omega}}\left( A_0^{\ast} A_1^{\ast} A_2 \exp{\left(-\mathrm{i} \Delta k z\right)} + \gamma_{11} A_1 A_1^{\ast} A_1 + 2\gamma_{12} A_2 A_2^{\ast} A_1\right) , \\
		\frac{\partial A_2}{\partial z} &= \im \frac{4 \pi \omega \chi^{\left(3\right)}}{c n_{2\omega}}\left( A_0 A_1^{2}\exp{\left(\mathrm{i} \Delta k z\right)}  + 2\gamma_{12} A_1 A_1^{\ast} A_2 + \gamma_{22} A_2 A_2^{*} A_2 \right).
	\end{aligned}
	\label{eq:anton_tuning_curve}
\end{equation}

В системе уравнений~\eqref{eq:anton_tuning_curve}, предполагается, что слабая мощность считывающего излучения накачки не влияет на $\chi^{(2)}$-решётку, поэтому представляется возможным использовать значения $A_0(z, t)$ из решения системы уравнений~\eqref{eq:anton}, соответствующей процессу записи решётки.



\section{Обсуждение результатов}

\begin{figure}[h]
	\centering
	\includegraphics[width=0.8\linewidth]{Dissertation/images/fibershg/kinetic}
	\caption{Временная зависимость эффективности ГВГ (оранжевая кривая: эксперимент, синяя кривая: моделирование}
	\label{fig:shg_kinetic}
\end{figure}

На рис.~\cref{fig:shg_kinetic} представлены экспериментальная кинетика эффективности ГВГ в процессе записи \(\chi^{(2)}\)-решётки в ОВ (согласно процедуре, описанной в разделе~\cref{sec:nonlin2/experiment}) и результаты ее моделирования с помощью системы уравнений~\eqref{eq:anton}. Расчётная кривая обрезана на уровне пиковой мощности ГВГ 30~мкВт, поскольку это соответствовало уровню паразитной засветки в эксперименте.


\begin{figure}[h!]
	\centering
	\includegraphics[width=0.99\linewidth]{Dissertation/images/fibershg/curves}
	\caption{Кривые фазового синхронизма, измеренные на различных этапах записи \(\chi^{(2)}\)-решётки. Синие и оранжевые кривые соответствуют спектральному и температурному синхронизмам соответственно, зеленые — результат моделирования}
	\label{fig:synches_exp_gathered}
\end{figure}


На рис.~\cref{fig:synches_exp_gathered} представлены экспериментально измеренные и рассчитанные кривые фазового синхронизма на различных этапах записи \(\chi^{(2)}\)-решётки. При измерениях мощность второй гармоники до момента времени 35~ч был пренебрежимо мал из-за малой мощности считывающего излучения накачки.

Кривые спектрального и температурного синхронизмов были пересчитаны в зависимость от величины фазовой расстройки $\Delta k$. Для этого использовались температурно-зависимая формула Селлмейера для плавленого кварца из работы~\cite{ghosh1994temperature}. Учёт волноводной дисперсии показателя преломления выполнялся для поперечной моды LP$_{01}$ на длине волны накачки и поперечной моды LP$_{12}$ на длине волны второй гармоники. Вариация эффективного показателя преломления на длине волны второй гармоники приводила к независящему от времени сдвигу положения пика кривой синхронизма, но не изменяла его форму.

Примечательно, что рассчитанные кривые фазового синхронизма воспроизводят асимметричную форму, наблюдаемую в эксперименте. Чтобы проверить, можно ли объяснить это явление влиянием эффекта Керра, был проведён расчёт кривых фазового синхронизма с постепенно уменьшающимися значениями значения керровских коэффициентов $\gamma_{11}, \gamma_{12}, \gamma_{22}$ вплоть до пренебрежимо малых величин. Фактически, эти коэффициенты характеризуют величину фазового вклада эффекта Керра в процесс ГВГ. Единственным наблюдаемым изменением стал не зависящий от времени сдвиг пикового значения кривой синхронизма, тогда как форма кривой осталась практически неизменной. Это качественно согласуется с результатами, представленными в работе \cite{bardal1991intensity}, где также показано, что керровский вклад приводит к сдвигу пика фазового синхронизма, но не изменяет формы самой кривой.


\begin{figure}[h!]
	\centering
	\includegraphics[width=0.99\linewidth]{Dissertation/images/fibershg/mc_dk}
	\caption{Временные зависимости сдвига фазового синхронизма (СФС) (a), ширины кривой на половине высоты (FWHM) (б) и пиковой эффективности в процессе формирования \(\chi^{(2)}\)-решётки (в). Синие и оранжевые точки — экспериментальные значения, полученные при измерениях спектральных и температурных КФС соответственно, зеленые кривые — результаты моделирования}
	\label{fig:mc_dk}
\end{figure}

\nomenclature{СФС}{Сдвиг фазового синхронизма}
\nomenclature{FWHM}{Full Width at Half Maximum}

Для количественного описания кривых фазового синхронизма были выбраны два параметра: сдвиг фазового синхронизма (СФС) и ширины кривой на половине высоты (FWHM). За нулевое значение СФС принята величина, соответствующая квазисинхронизму. Для спектральной кривой это достигается при длине волны накачки, использовавшейся при записи, а для температурной — при температуре термостата в процессе записи \(\chi^{(2)}\)-решётки. Зависимости этих параметров от времени представлены на рис.~\cref{fig:mc_dk}a и рис.~\cref{fig:mc_dk}б.

Экспериментальное значение СФС составило 30~м\(^{-1}\) в момент времени \(t = 35\)~ч и впоследствии уменьшилось до 15~м\(^{-1}\) к моменту \(t = 68\)~ч. Модель также предсказывает монотонное уменьшение СФС при \(t > 35\)~ч с асимптотическим приближением к постоянному значению.

Экспериментальное значение FWHM также уменьшалось с 30~м\(^{-1}\) при \(t = 35\)~ч до постоянного значения 15~м\(^{-1}\) при \(t = 68\)~ч, что согласуется с предсказаниями модели. 

Расхождения экспериментальных и модельных данных на ранней стадии формирования \(\chi^{(2)}\)-решетки (\(t < 39\)~ч) могут быть объяснены рядом факторов, изложенных в работе~\cite{antonyuk2001self}, но не учтённых в упрощённой модели~\eqref{eq:anton}. Согласно микроскопической модели, на начальных этапах важную роль играют формирование ЭПЗ с различными расстояниями переноса электронов с различными временами релаксации, а также неоднородное уширение и конкуренция между переходами Ge-центр–матрица и Ge-центр–Ge-центр. Данные механизмы приводят к наличию временной зависимости комбинации параметров $(\alpha_2 u^{2})^{-1}$, отвечающих за скорость формирования \(\chi^{(2)}\)-решетки, тогда как в упрощённой модели~\eqref{eq:anton} оба параметра от времени не зависят. Кроме того, пространственное распределение постоянного электрического поля и поля второй гармоники также могут изменяться во времени, что приведёт к временной зависимости параметров $\gamma_{11}$, $\gamma_{12}$, $\gamma_{22}$.

Согласие модельных и экспериментальных данных в области насыщения (\(t > 39\)~ч) может быть объяснено тем, что к этому моменту времени указанные выше временные зависимости играют значительно меньшую роль, а стационарные значения параметров подобраны для наилучшего соответствия модели и эксперимента. 

Уменьшение ширины кривой фазового синхронизма связано с соответствующим увеличением длины \(\chi^{(2)}\)-решётки. Этот эффект, наряду со сдвигом фазового синхронизма, должен приводить к снижению эффективности ГВГ. Однако, как показано на рис.~\cref{fig:shg_kinetic}, после 40~ч она оставалась практически постоянной. Это объясняется одновременным увеличением пикового значения кривой синхронизма (см. рис.~\cref{fig:mc_dk}c). Таким образом, установившаяся эффективность ГВГ свидетельствует о динамическом равновесии между спектральной расстройкой и одновременным ростом пиковой эффективности ГВГ. Это свидетельствует о сложном, нестационарном характере \(\chi^{(2)}\)-решётки.

\section{Выводы к Главе 3}

\begin{itemize}
	\item Впервые экспериментально исследована эволюция спектральных и температурных кривых фазового синхронизма при генерации второй гармоники в процессе оптического полинга германосиликатного оптического волокна без затравочного излучения второй гармоники. Показано, что в ходе формирования 
	\(\chi^{(2)}\)-решётки происходит монотонное уменьшение ширины КФС (FWHM) и сдвига фазового синхронизма (СФС) до выхода на стационарные значения, при этом форма кривых синхронизма сохраняет выраженную асимметрию.
	
	\item Впервые выполнена адаптация модели Антонюка \cite{antonyuk2001self} для описания эволюции кривых фазового синхронизма путём введения фазовой расстройки $\Delta k$ в укороченные уравнения ГВГ и учёта керровского эффекта. Подбором параметров \(u, \alpha_2, \gamma_{11}, \gamma_{12}, \gamma_{22}\) и начальной мощности второй гармоники достигнуто удовлетворительное согласие модели с экспериментом по эффективности ГВГ, ширине и положению пика КФС.
	
	\item Установлено, что на ранней стадии формирования 
	\(\chi^{(2)}\)-решётки (\(t < 39\)~ч) наблюдаются расхождения между экспериментом и моделью, обусловленные микроскопическими механизмами, не учтёнными в упрощённой модели: распределением времен релаксации экситонов с переносом заряда, неоднородным уширением и конкуренцией переходов Ge-центр–матрица и Ge-центр–Ge-центр. В области насыщения (\(t > 39\)~ч) влияние этих факторов ослабевает, и модель правильно воспроизводит стационарные параметры КФС.
	
	\item Обнаружено, что при уменьшении ширины кривых фазового синхронизма и сдвиге их пика эффективность ГВГ на поздних стадиях остаётся практически постоянной. Это объясняется одновременным ростом пикового значения КФС, что свидетельствует о наличии динамического равновесия между спектральной расстройкой и увеличением пиковой эффективности. Данный эффект указывает на сложный, нестационарный характер формирования \(\chi^{(2)}\)-решётки.
\end{itemize}

\FloatBarrier